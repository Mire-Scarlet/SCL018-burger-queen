import React, { Fragment } from 'react';

const WaitersOrder = () => {
  return <Fragment>
    Aquí debe imprimirse el menú de orden (precio + cantidad)
  </Fragment>;
};

export default WaitersOrder;
